import { styled } from '@mui/system'
import colors from '@styles/ThemeProvider/colors'

const BlockEffect = styled('div')({
  cursor: 'pointer',
  color: colors.actionSecondary,
})

export { BlockEffect }
