import { styled } from '@mui/system';
import { Box } from '@mui/material';

const Container = styled(Box)({
  marginTop: '16px',
  padding: 0,
  marginRight: 0,
  width: '100%',
  height: '191px',
});
export { Container };
